{
  "name": "obsidian-auto-link-title",
  "version": "1.5.6",
  "description": "Automatically fetches the titles of links from the web and formats link when pasted",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "bun build src/main.ts --outfile=main.js --format=cjs --external=obsidian --external=electron --sourcemap=inline --watch",
    "build": "bun build src/main.ts --outfile=main.js --format=cjs --external=obsidian --external=electron --sourcemap=inline --minify --banner=\"/* THIS IS A GENERATED/BUNDLED FILE BY BUN */\"",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "bun test"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "@types/bun": "^1.3.3",
    "@types/node": "^24.10.1",
    "linkedom": "^0.18.12",
    "obsidian": "^1.10.3",
    "typescript": "^5.9.3"
  }
}
